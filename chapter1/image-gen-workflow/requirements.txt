# 实验 1-4 文生图工作流与原生图像生成的对照

# 核心依赖
openai>=1.12.0        # 改写节点（Mimo，OpenAI 兼容接口）和 GPT-Image 2
requests>=2.31.0      # DashScope 万相、Qwen Image 与图片下载
python-dotenv>=1.0.0  # 环境变量管理

# 开发依赖
pytest>=7.4.0         # 测试框架
